{
  "name": "statusbar-toggle",
  "displayName": "statusbar-toggle",
  "description": "toggle the status bar with the tags",
  "publisher": "sam44323",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sam44323/status-bar-toggle"
  },
  "bugs": {
    "url": "https://github.com/Sam44323/status-bar-toggle/issues"
  },
  "icon": "icon.png",
  "contributes": {
    "commands": [
      {
        "command": "statusbarToggle.selectState",
        "title": "Select Status Bar State"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.103.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
